#!/bin/bash

PID=$(cat /tmp/tobobrowse.pid 2>/dev/null)
if [ -n "$PID" ]; then
  kill $PID 2>/dev/null && echo "Restarting server..." || echo "Starting server..."
else
  echo "Starting server..."
fi
python daemon.py
